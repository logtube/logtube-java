# project information
project: logtube-java-test
env: test

# Console output
console:
  enabled: true
  topics:
    - info
    - debug
    - x-access
    - x-redis

# PlainFile output
plain:
  enabled: true
  dir: /tmp/logtube-java-test
  topics:
    - info
    - debug

# JSONFile output
json:
  enabled: true
  dir: /tmp/logtube-java-test
  topics:
    - x-access
    - x-redis

# Remote output
remote:
  enabled: true
  hosts:
    - 127.0.0.1:9922
  topics:
    - info
    - debug
    - x-access
    - x-redis
